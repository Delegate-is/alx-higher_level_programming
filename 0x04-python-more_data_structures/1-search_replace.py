#!/usr/bin/python3
def search_replace(my_list, search, replace):
    copy = []
    for n in range(len(my_list)):
        if my_list[i] == search
            copy.append(replace)
        else:
            copy.append(my_list[n])
    return copy
